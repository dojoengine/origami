mod libraries {
    mod events;
}

mod models {
    mod delegate;
    mod delegator;
    mod token;
}

mod systems {
    mod delegate {
        mod contract;
        mod interface;
        mod tests;
    }
    mod delegator {
        mod contract;
        mod interface;
        mod tests;
    }
    mod token {
        mod contract;
        mod interface;
        mod tests;
    }
}

