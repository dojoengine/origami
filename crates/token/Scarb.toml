[package]
name = "origami_token"
version = "1.0.0-alpha.0"
description = "Implementations of ERC standards for the Dojo framework."
homepage = "https://github.com/dojoengine/origami/tree/crates/token"

[dependencies]
dojo.workspace = true

[lib]

[[target.dojo]]

[tool.dojo.world]
name = "token"
seed = "token"
namespace = { default = "origami_token" }
