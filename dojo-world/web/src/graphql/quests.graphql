query Quests {
  dojoWorldQuestModels {
    edges {
      node {
        id
        name
        desc
        image_uri {
          Some
        }
        quest_type
        external {
          Some
        }
      }
    }
  }
}

